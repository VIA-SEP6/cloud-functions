// const { db } = require("../../util/adminDbUtil");
// const { weekAgo } = require("./util");

// const weeklyAggregate = (topic) => {
//     const topic
//   return db.collection(`${topic}`).where("timestamp", "<=", new Date()).get().then((querySnapshot)=>{

//   });
// };

// module.exports = { weeklyAggregate };
